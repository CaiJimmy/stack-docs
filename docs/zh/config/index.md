# 介绍

Hugo 接受 `TOML`、`YAML` 和 `JSON` 作为配置格式。从 v4 开始，此主题使用 **TOML** 作为标准配置格式。

如果您使用的是 `hugo-theme-stack-starter` 模板，您可以在 `config/_default/` 文件夹下找到配置文件。它们被分成了不同的文件（如 `hugo.toml`、`params.toml`、`menu.toml` 等），以便更容易找到您想要修改的配置。

可用配置的完整列表可以在此主题仓库根目录下的 `config/_default/` 文件夹中找到。
